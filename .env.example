# Server configuration
HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=*

# Security & Admin (set a strong random string to enable GET /api/admin/messages)
ADMIN_TOKEN=

# Database
DB_PATH=messages.db

# Optional SMTP notification settings (leave empty if not using email dispatch)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_TO=najeebullah.shaikh084@gmail.com
